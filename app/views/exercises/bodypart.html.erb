<h1>Bodypart Exercises</h1>

<!-- Would like to get this self selecting by params in one command, to match the URL.
	Can't get this working - partial doesn't help as table doesn't load. Maybe a helper? At the moment, a LOT of code is being repeated -->
	<!-- or a helper - too tired to work on this now -->
</div><%= link_to 'Generate Workout', generator_path %></div><div><%= link_to 'Index', exercises_path %></div>




<br>
<table>
  <thead>
    <tr>
      <th>Move</th>
      <th>Bodypart</th>
      <th>Reps</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>



   <tbody>
    <% if current_page?('/generator/legs') %>
        <% @legs.each do |exercise| %>
        <tr>
            <td><%= exercise.move.titleize %></td>
            <td><%= exercise.bodypart.titleize %></td>
            <td><%= exercise.reps %></td>
            <% if exercise.description == nil %><td>-</td><% else %><td><%= exercise.description %></td><% end %> 
            <td><!--<a href='/exercises/<#%= exercise[:id] %>'>show 2</a>--><%= link_to 'Show', exercise %></td>
            <td><%= link_to 'Edit', edit_exercise_path(exercise) %></td>
            <td><%= link_to 'Destroy', exercise, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
    <% end %>
   </tbody>

  <tbody>
    <% if current_page?('/generator/full') %>
        <% @full.each do |exercise| %>
        <tr>
            <td><%= exercise.move.titleize %></td>
            <td><%= exercise.bodypart.titleize %></td>
            <td><%= exercise.reps %></td>
            <% if exercise.description == nil %><td>-</td><% else %><td><%= exercise.description %></td><% end %> 
            <td><!--<a href='/exercises/<#%= exercise[:id] %>'>show 2</a>--><%= link_to 'Show', exercise %></td>
            <td><%= link_to 'Edit', edit_exercise_path(exercise) %></td>
            <td><%= link_to 'Destroy', exercise, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
    <% end %>
   </tbody>

   <tbody>
    <% if current_page?('/generator/shoulders') %>
        <% @shoulders.each do |exercise| %>
        <tr>
            <td><%= exercise.move.titleize %></td>
            <td><%= exercise.bodypart.titleize %></td>
            <td><%= exercise.reps %></td>
            <% if exercise.description == nil %><td>-</td><% else %><td><%= exercise.description %></td><% end %> 
            <td><!--<a href='/exercises/<#%= exercise[:id] %>'>show 2</a>--><%= link_to 'Show', exercise %></td>
            <td><%= link_to 'Edit', edit_exercise_path(exercise) %></td>
            <td><%= link_to 'Destroy', exercise, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
    <% end %>
   </tbody>

  <tbody>
    <% if current_page?('/generator/back') %>
        <% @back.each do |exercise| %>
        <tr>
            <td><%= exercise.move.titleize %></td>
            <td><%= exercise.bodypart.titleize %></td>
            <td><%= exercise.reps %></td>
            <% if exercise.description == nil %><td>-</td><% else %><td><%= exercise.description %></td><% end %> 
            <td><!--<a href='/exercises/<#%= exercise[:id] %>'>show 2</a>--><%= link_to 'Show', exercise %></td>
            <td><%= link_to 'Edit', edit_exercise_path(exercise) %></td>
            <td><%= link_to 'Destroy', exercise, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
    <% end %>
   </tbody>

  <tbody>
    <% if current_page?('/generator/core') %>
        <% @core.each do |exercise| %>
        <tr>
            <td><%= exercise.move.titleize %></td>
            <td><%= exercise.bodypart.titleize %></td>
            <td><%= exercise.reps %></td>
            <% if exercise.description == nil %><td>-</td><% else %><td><%= exercise.description %></td><% end %> 
            <td><!--<a href='/exercises/<#%= exercise[:id] %>'>show 2</a>--><%= link_to 'Show', exercise %></td>
            <td><%= link_to 'Edit', edit_exercise_path(exercise) %></td>
            <td><%= link_to 'Destroy', exercise, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
    <% end %>
   </tbody>

  <tbody>
    <% if current_page?('/generator/chest') %>
        <% @chest.each do |exercise| %>
        <tr>
            <td><%= exercise.move.titleize %></td>
            <td><%= exercise.bodypart.titleize %></td>
            <td><%= exercise.reps %></td>
            <% if exercise.description == nil %><td>-</td><% else %><td><%= exercise.description %></td><% end %> 
            <td><!--<a href='/exercises/<#%= exercise[:id] %>'>show 2</a>--><%= link_to 'Show', exercise %></td>
            <td><%= link_to 'Edit', edit_exercise_path(exercise) %></td>
            <td><%= link_to 'Destroy', exercise, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
    <% end %>
   </tbody>

</table>

<br>


    		

    
    	