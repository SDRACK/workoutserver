<!--<h1>Exercise Database</h1>

#<%= link_to 'New Exercise', new_exercise_path %><div> </div><%= link_to 'Generate Workout', generator_path %>
<br>
#<% Exercise::BODYPART_OPTIONS.each do |x| %>
#  <%= link_to x, "generator/#{x.downcase}" %>
#<% end %>
<br>
<h4>All Exercises</h4>
<table>
  <thead>
    <tr>
      <th>Move</th>
      <th>Bodypart</th>
      <th>Reps</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
 #   <% @exercises.each do |exercise| %>
      <tr>
 #       <td><%= exercise.move.titleize %></td>
 #       <td><%= exercise.bodypart.titleize %></td>
 #       <td><%= exercise.reps %></td>
 #       <% if exercise.description == nil %><td>-</td><% else %><td><%= exercise.description %></td><% end %> 
 #       <td><!--<a href='/exercises/<#%= exercise[:id] %>'>show 2</a>--><!--<%= link_to 'Show', exercise %></td>-->
        <!--<td><%= link_to 'Edit', edit_exercise_path(exercise) %></td>
 #       <td><%= link_to 'Destroy', exercise, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
 #   <% end %>
  </tbody>
</table>

<br>-->


<!--<#% form_tag '/exercises/pick_bp' do %>
#    <#%= select_tag 'bodypart_selector', options_for_select(Exercise::BODYPART_OPTIONS), prompt: "Select a bodypart" %>
#      <#%= submit_tag 'Submit' %>
#<#% end %>



<!--   <div class="field">
    <= f.label 'bp' ><br>
    <= f.select 'bp', Exercise::BODYPART_OPTIONS, prompt: "Select a bodypart" >
  </div>
< end > 






#could this be 'form_for' and then an instance variable in index?
<#%= form_tag exercises_path, class: 'bodypart_selector', method: 'get' do %>
      <#%= select_tag 'bodypart_selector', options_for_select(Exercise::BODYPART_OPTIONS), prompt: "Select a bodypart" %>
      <#%= submit_tag 'submit' %>
<#% end %>


     <#%= @legs.inspect %> -->

 
      <!-- /Header -->
      
      <div id="page">
        <div class="container">
          <div class="row">
            <div class="3u">
              <section id="sidebar1">
                <header>
                  <h2>Introduction</h2>
                </header>
                <ul class="style3">
                  <li class="first">
                    <p>Welcome to Steve Rackham's workout generator.</p>
                    <p>Click 'Generate Workout, or select a bodypart in the right column, to begin.</p>
                    <% if notice %>
                      <p class="notice"><%= notice %></p>
                    <% end %>
                    <p class="button"><%= link_to 'Generate Workout', generator_path %></p>
                  </li>
               
                </ul>
              </section>
            </div>
            <div class="6u skel-cell-important">
              <section id="content" >
                <header>
                  <h2>All Exercises</h2>
                </header>
                <ul class="style3">
                  <li class="first">
                <% if admin_signed_in? %><p class="button"><%= link_to 'New Exercise', new_exercise_path %></p><% end %>
                </li>
               
                </ul>
                  <table>
                    <thead>
                      <tr>
                        <th>Move</th>
                        <th>Bodypart</th>
                        <th>Reps</th>
                        <th>Description</th>
                        <% if admin_signed_in? %><th colspan="3"></th><% end %>
                      </tr>
                    </thead>

                    <tbody>
                      <% @exercises.each do |exercise| %>
                        <tr>
                          <td><%= exercise.move.titleize %></td>
                          <td><%= exercise.bodypart.titleize %></td>
                          <td><%= exercise.reps %></td>
                          <% if exercise.description == nil %><td>-</td><% else %><td><%= exercise.description %></td><% end %> 
                          <!--<td><!--<a href='/exercises/<#%= exercise[:id] %>'>show 2</a>--><!--<#%= link_to 'Show', exercise %></td>-->
                          <% if admin_signed_in? %><td><%= link_to 'Edit', edit_exercise_path(exercise) %></td>
                          <td><%= link_to 'Delete', exercise, method: :delete, data: { confirm: 'Are you sure?' } %></td><% end %>
                        </tr>
                      <% end %>
                    </tbody>
                  </table></p>
                
              </section>
            </div>
            <div class="3u">
              <section id="sidebar2">
                <header>
                  <h2>Bodypart Workouts</h2>
                </header>
                <ul class="style1">
                  <% Exercise::BODYPART_OPTIONS.each do |x| %>
                    <li class="button"><span></span><%= link_to x, "generator/#{x.downcase}" %></li>
                  <% end %>
                </ul>
                <!--<a href="#" class="button">Full Article</a>-->
              </section>
            </div>
          </div>

        </div>  
      </div>

